# Kalkulator Interaktif

Kalkulator web interaktif yang responsif dan mudah digunakan, dilengkapi
fitur lengkap mulai dari operasi dasar, history, hingga memory function.
Aplikasi ini dapat dijalankan langsung melalui browser tanpa instalasi
tambahan.

## ✨ Fitur Utama

### **1. Antarmuka Pengguna**

-   Display utama untuk menampilkan angka & hasil perhitungan.
-   Tombol angka 0--9.
-   Tombol operator dasar: **+**, **−**, **×**, **÷**.
-   Tombol **C (Clear)** dan **CE (Clear Entry)**.
-   Tombol **= (Equals)**.
-   Tombol **titik desimal (.)**.
-   Desain responsif (mobile & desktop).

### **2. Fungsionalitas**

-   Operasi aritmatika dasar: tambah, kurang, kali, bagi.
-   Mendukung **perhitungan berantai**, contoh: `5 + 3 × 2`.
-   Menggunakan **prioritas operator yang benar**.
-   Penanganan error seperti **pembagian dengan nol**.
-   **Keyboard support**:
    -   Angka (0--9)
    -   Operator (+ - \* /)
    -   `Enter` = sama dengan
    -   `Backspace` = hapus satu digit
    -   `Escape` = Clear (C)

### **3. Fitur Lanjutan**

-   **History**: menyimpan **5 perhitungan terbaru**.
-   **Memory Functions**:
    -   `M+` → Tambah ke memori\
    -   `M-` → Kurangi dari memori\
    -   `MR` → Menampilkan nilai memori\
    -   `MC` → Hapus memori

## 📱 Desain Responsif

Menyesuaikan tampilan untuk smartphone, tablet, dan desktop.

## 🚀 Cara Menjalankan Aplikasi

1.  Download atau clone repository.
2.  Buka file `index.html` di browser.

## 🧩 Struktur File

    / (root)
    │── index.html
    │── styles.css
    │── script.js
    └── README.md

## 🛠 Cara Upload ke GitHub

``` bash
git init
git add .
git commit -m "Initial commit — Kalkulator Interaktif"
git branch -M main
git remote add origin https://github.com/USERNAME/NAMA-REPO.git
git push -u origin main
```
